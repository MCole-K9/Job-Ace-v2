import {json} from '@sveltejs/kit';

export async function GET (){

}

export async function POST(){
    // authenticate user
    // then, check to see that they are an admin (does subapabse have 
    // transactions? need to look into it)
    // also need to look into the validation whatevers
}